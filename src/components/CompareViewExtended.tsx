// 比較モード拡張（同期/個別再生・コメント表示）
import React from 'react';

export const CompareViewExtended: React.FC = () => {
  return (
    <div className='compare-view-extended'>
      {/* 同期/個別再生切替・コメント表示（読み取りのみ） */}
    </div>
  );
};

